import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => <div>API-kanri Frontend 初期セットアップ完了</div>;

ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
